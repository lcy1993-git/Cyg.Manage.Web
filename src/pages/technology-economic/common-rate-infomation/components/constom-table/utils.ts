
export const api = {}

export const formatData = (type: string) => {
  switch (x) {
    case "x":
      return {

      };
  }
  const columns: any[] = [];
  const dataSource: any[] = [];
  return {
    columns,
    dataSource
  }
}
