import React from 'react';
import { useRequest } from 'ahooks';
import { getCompanyGroups } from '@/services/personnel-config/work-handover';

const GroupIdentity: React.FC = () => {
  return (
    <>
      <div>11</div>
    </>
  );
};

export default GroupIdentity;
